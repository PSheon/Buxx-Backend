/**
 * calculate-team-share-log controller
 */

import { factories } from "@strapi/strapi";

export default factories.createCoreController(
  "api::calculate-team-share-log.calculate-team-share-log",
  ({ strapi }) => ({})
);
