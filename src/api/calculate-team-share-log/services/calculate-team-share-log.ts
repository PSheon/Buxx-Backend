/**
 * calculate-team-share-log service
 */

import { factories } from "@strapi/strapi";

export default factories.createCoreService(
  "api::calculate-team-share-log.calculate-team-share-log",
  ({ strapi }) => ({})
);
