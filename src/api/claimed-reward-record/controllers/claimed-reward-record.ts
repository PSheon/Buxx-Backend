/**
 * claimed-reward-record controller
 */

import { factories } from "@strapi/strapi";

export default factories.createCoreController(
  "api::claimed-reward-record.claimed-reward-record",
  ({ strapi }) => ({})
);
