/**
 * claimed-reward-record service
 */

import { factories } from "@strapi/strapi";

export default factories.createCoreService(
  "api::claimed-reward-record.claimed-reward-record",
  ({ strapi }) => ({})
);
